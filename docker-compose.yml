services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: "3102"
      DB_PATH: /app/data/app.db
    ports:
      - "${PUBLIC_BIND:-0.0.0.0}:${PUBLIC_PORT:-3102}:3102"
    volumes:
      - canvas-data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  canvas-data:
