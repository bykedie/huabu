# Relay Tests Worker Status

- Time: 2026-08-01 20:27:07 +08:00
- Status: complete
- Completed: Updated all text, image, and video runtime tests to use the authenticated user's key while retaining the prior billing, idempotency, refund, SSRF, async quota, and recovery assertions. Added a focused three-key lifecycle/isolation test covering save, replace, test, clear, no-key blocking, configured booleans, exact public fields, user isolation, address/model-only administrator configuration, text model discovery normalization/error handling, model limits, whitelist rejection, secret-free observable data, and legacy shared-key non-revival. Added loopback-only reflection regressions for video create, poll, and download errors plus successful text payloads whose content or nested usage data reflects the current user's key. The regressions scan API responses, captured console errors, audit rows, generation rows, ledger rows, and encrypted-key rows for both raw and URL-encoded secret variants without weakening configured booleans. Production tests require Compose to neither declare nor expand legacy shared API-key variables, validate current address/model-only `h` and maintenance behavior, cover public-HTTP UUID fallback, and verify three-column migration/strict recovery.
- Next: Main thread integration and final acceptance only.
- Blockers: None. The tested `server/app.js` version was last updated at 2026-08-01 20:22:58 +08:00 and applies key-aware redaction to video create/poll/download errors plus text success `content` and `usage` validation.
- Verification: Reflection focus passed 2/2; `node --test tests/server.test.mjs` passed 19/19; `node --test tests/production-config.test.mjs` passed 12/12; `npm.cmd test` passed 32/32; `npm.cmd run build` passed and produced `dist/assets/index-DDen9KmM.js` plus `dist/assets/index-ZMfIRUE0.css`; the post-build production scan passed 12/12; `git diff --check` passed with only pre-existing LF/CRLF conversion warnings. Video create and poll failures refund exactly once, video download failures remain pending with the reservation intact, and both text reflection failures fully refund their reservations. No real external network was used.
