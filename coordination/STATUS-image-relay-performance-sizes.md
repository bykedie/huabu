# Image Relay, Performance, and Sizes Status

## Checkpoint: 2026-08-03 13:22 +08:00

- 当前目标：移除文字、图片、视频中转站的新部署默认地址，定位并修复当前代码造成的生图额外等待，补充常见生图比例与分辨率，同时保持三用户密钥、管理员开放模型、参考图编辑、幂等、持久化和安全契约。
- 已完成：恢复 Codex 活动目标；按规定读取项目恢复文档并检查实际 Git、代码热点和运行端口；确认本地与 `origin/codex/infinite-canvas` 均为 `302b1b10a1922ff651c421ad51bafe677a8eeb0a`、起始工作树干净；确认真实 API `3102` 和前端 `5182` 返回 HTTP 200，`3000/5174` 未占用；确认图片地址仍有固定域名回退，而文字和视频新部署默认为空；确认当前前后端只接受 `1024x1024`、`1536x1024`、`1024x1536`；初步拆出生图等待阶段为画布保存、参考图预处理、服务端地址校验、上游生成和结果后处理。
- 未完成：检查参考仓库现有比例/尺寸选项；为各阶段建立隔离 mock relay 耗时基线；添加新部署空地址、显式数据库地址保留、常见尺寸透传和性能修复契约；实施最小修改；运行聚焦测试、完整测试、build 和差异检查；完成隔离浏览器验收、夹具清理和最终检查点。
- 最后验证结果：本目标尚未修改产品代码或运行新基线测试；上一交付的聚焦测试为 `20/20`、完整测试为 `61/61`，build 和 `git diff --check` 通过。当前只读健康检查确认 `3102/5182` 均为 HTTP 200。
- 修改文件：新增本文件，并同步 `GOALS.md`、`PLAN.md`、`STATUS.md`；起始工作树干净，当前尚未修改产品代码、测试、环境示例或部署配置。
- 下一步：只读检查本地参考仓库的比例/尺寸实现和当前 `flushRef`、参考图预处理、服务端图片转发路径，然后用隔离 mock relay 记录各阶段耗时，先写失败契约再修改实现。
- 恢复命令：`Set-Location 'C:\Users\Administrator\Documents\无限画布'`; `Get-Content -Encoding UTF8 HANDOFF.md`; `Get-Content -Encoding UTF8 GOALS.md`; `Get-Content -Encoding UTF8 PLAN.md`; `Get-Content -Encoding UTF8 STATUS.md`; `Get-Content -Encoding UTF8 coordination/STATUS-image-relay-performance-sizes.md`; `git status --short --branch`; `git diff -- GOALS.md PLAN.md STATUS.md coordination/STATUS-image-relay-performance-sizes.md server/app.js src/App.tsx tests .env.example docker-compose.yml`; `rg -n "AI_IMAGE_BASE_URL|ImageSize|flushRef|referenceImageDataUrl|response_format|output_format" server src tests .env.example docker-compose.yml`。

## Checkpoint: 2026-08-03 14:52 +08:00

- 当前目标：完成新部署空中转地址、生图本地性能修复和常见比例/分辨率，保持管理员已保存地址、三用户密钥、开放模型、参考图、幂等、媒体持久化和安全契约；当前目标仍 active，不推送、不部署生产。
- 已完成：移除 `server/app.js`、`.env.example`、Compose 的固定图片地址回退；新部署三类地址/模型为空且 SQLite 显式管理员配置继续优先；新增统一客户端图片预设与服务端同值白名单；画布保存与参考图预处理并行；图片密钥测试和正式生成使用图片类型超时；正式图片超时/恢复独立为 300/360 秒，保留旧文字 120/180 秒 `.env` 兼容；Nginx 为 310 秒；编辑请求不再强制 `response_format=b64_json`/`output_format=png`；上游 base64 PNG/JPEG/WebP 原始字节进入 `media` 并返回签名 URL，图片资产可直接保存该 URL；图片/视频共用 512 MiB 媒体配额，图片 JSON 上限默认 50 MiB。已同步 README、AGENTS、SPEC、PROJECT_MEMORY、DECISIONS、HANDOFF 和主目标/计划/状态。
- 未完成：在临时数据库和本地 mock relay 上完成桌面与 `390x844` 浏览器验收，核对尺寸下拉、参考图 multipart、4K 值、结果节点/媒体 URL、布局/溢出、console/失败请求；关闭标签并停止/删除夹具；运行最终 `npm.cmd test`、build、`git diff --check`、秘密/NUL/临时资产/真实服务健康检查；写最终检查点并标记目标完成。
- 最后验证结果：AI 创作契约 `22/22`、生产配置 `12/12`、中转现代化 `5/5`、服务端 `20/20` 通过；`node --check server/app.js server/db.js server/index.js` 通过；一次 `npm.cmd run build` 通过，产物为 `index-BX6NdpM7.css` 与 `index-B51-mZWZ.js`，仅有既有 500 kB chunk warning；`git diff --check` 在较早实现点退出 0。服务端测试覆盖 4K 横竖透传、编辑表单不强制格式、base64 原字节签名读取、图片资产保存和图片恢复阈值独立。
- 修改文件：`.env.example`、`AGENTS.md`、`README.md`、`GOALS.md`、`PLAN.md`、`STATUS.md`、`HANDOFF.md`、`PROJECT_MEMORY.md`、`SPEC.md`、`DECISIONS.md`、`deploy/nginx.conf`、`docker-compose.yml`、`server/app.js`、`server/db.js`、`server/index.js`、`src/App.tsx`、新增 `src/image-sizes.ts`、四个聚焦测试文件和本协调状态文件。
- 下一步：先建立隔离 fixture（新临时目录、API/前端/relay 非真实端口、虚构账号/密钥），用内置浏览器验收桌面和移动端的图片预设、参考图和生成结果；确认后立即清理 fixture，再跑最终完整门禁与最终七字段检查点。
- 恢复命令：`Set-Location 'C:\Users\Administrator\Documents\无限画布'`; `git status --short --branch`; `git diff --check`; `Get-Content -Encoding UTF8 HANDOFF.md`; `Get-Content -Encoding UTF8 GOALS.md`; `Get-Content -Encoding UTF8 PLAN.md`; `Get-Content -Encoding UTF8 STATUS.md`; `Get-Content -Encoding UTF8 coordination/STATUS-image-relay-performance-sizes.md`; `git diff -- server/app.js server/db.js server/index.js src/App.tsx src/image-sizes.ts .env.example docker-compose.yml deploy/nginx.conf tests README.md AGENTS.md PROJECT_MEMORY.md SPEC.md DECISIONS.md`; `node --test tests/ai-creation-contract.test.mjs`; `node --test tests/production-config.test.mjs`; `node --test tests/relay-modernization.test.mjs`; `node --test tests/server.test.mjs`。

## Checkpoint: 2026-08-03 15:34 +08:00

- 当前目标：完成新部署空中转地址、生图代码侧等待修复和常见比例/分辨率，并在不触碰真实服务、生产或用户凭据的前提下完成全部自动化、浏览器、清理和交接门禁；本地目标现已完成，改动尚未提交、推送或部署。
- 已完成：新部署文字/图片/视频中转地址与模型列表为空，SQLite 中管理员明确保存的地址继续优先；图片正式请求/遗留恢复独立为 300/360 秒，Nginx 为 310 秒；画布保存和参考图预处理并行；编辑不强制 PNG/base64；上游 base64 PNG/JPEG/WebP 原始字节进入 `media` 并返回签名 URL；客户端/服务端共同支持 `auto`、常见 1K、2K 和 4K 共 13 个尺寸。隔离桌面验收确认 13 项、`3840x2160`、1 张参考图、multipart `/v1/images/edits`、mock 上游约 180 ms、端到端约 339 ms、签名媒体结果与资产收藏；`390x844` 验收确认 `2160x3840` 可选、参考图状态、长标签/创作框/资源面板/Dock 无冲突、文档宽度 `390/390`、console warning/error 为空。
- 未完成：本目标没有剩余实现、验证或夹具清理；Git 提交、GitHub 推送和生产部署不在本目标范围，后续如由用户要求必须作为新目标单独执行。
- 最后验证结果：聚焦 AI 创作 `22/22`、生产配置 `12/12`、中转现代化 `5/5`、服务端 `20/20`；最终 `npm.cmd test` 通过 `64/64`；最终 `npm.cmd run build` 通过，产物 `index-BX6NdpM7.css`、`index-B51-mZWZ.js`，仅有既有 500 kB chunk warning；`node --check server/app.js server/db.js server/index.js`、`git diff --check`、变更文件 NUL/秘密形态检查通过。浏览器标签关闭、viewport 复位，PID `21384/18004` 停止，`.codex-acceptance-image-performance` 删除，`3152/3153` 无监听；真实 `3102/5182` 最终 HTTP 200。固定图片域名仅存在于未修改的历史目标/兼容记录，不再存在于运行时默认或新部署配置。
- 修改文件：`.env.example`、`AGENTS.md`、`DECISIONS.md`、`GOALS.md`、`HANDOFF.md`、`PLAN.md`、`PROJECT_MEMORY.md`、`README.md`、`SPEC.md`、`STATUS.md`、`deploy/nginx.conf`、`docker-compose.yml`、`server/app.js`、`server/db.js`、`server/index.js`、`src/App.tsx`、新增 `src/image-sizes.ts`、`tests/ai-creation-contract.test.mjs`、`tests/production-config.test.mjs`、`tests/relay-modernization.test.mjs`、`tests/server.test.mjs` 和本协调状态文件。起始基线工作树干净，当前这些未提交修改均属于本目标；不得 reset、checkout 或覆盖。
- 下一步：等待用户的新指令；若要求提交/推送，先只读复核本检查点、`git status`、完整差异与远端状态，再建立独立交付目标，不要把本地完成误称为 GitHub 或生产完成。
- 恢复命令：`Set-Location 'C:\Users\Administrator\Documents\无限画布'`; `Get-Content -Encoding UTF8 HANDOFF.md`; `Get-Content -Encoding UTF8 GOALS.md`; `Get-Content -Encoding UTF8 PLAN.md`; `Get-Content -Encoding UTF8 STATUS.md`; `Get-Content -Encoding UTF8 coordination/STATUS-image-relay-performance-sizes.md`; `git status --short --branch`; `git diff --check`; `git diff -- .env.example AGENTS.md DECISIONS.md GOALS.md HANDOFF.md PLAN.md PROJECT_MEMORY.md README.md SPEC.md STATUS.md deploy/nginx.conf docker-compose.yml server/app.js server/db.js server/index.js src/App.tsx src/image-sizes.ts tests/ai-creation-contract.test.mjs tests/production-config.test.mjs tests/relay-modernization.test.mjs tests/server.test.mjs coordination/STATUS-image-relay-performance-sizes.md`; `npm.cmd run build`; `npm.cmd test`; `Invoke-WebRequest -UseBasicParsing http://127.0.0.1:3102/`; `Invoke-WebRequest -UseBasicParsing http://127.0.0.1:5182/`。
