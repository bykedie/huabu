# 无限画布项目交接文档

> 本文是人类与 AI 协作者的项目级交接、恢复和共同研发手册。
> 它不能代替检查当前代码、Git 差异、状态文件和测试结果。
> 最后同步时间：2026-08-02，Asia/Shanghai。

## 一、项目使命

无限画布的产品名是“墨屿画布”。这是一个可自部署、保留商业兼容能力的无限画布产品，不是静态演示页面；当前版本默认关闭站内计费与充值界面。

用户可以在画布中组织文本、便签、图片、视频、AI 生成配置、创作框架和曲线连接。商业能力包括账号、积分、兑换码、人工充值审核、中转站管理、资产存储和管理员审计。

当前版本已经完成参考视觉对齐、普通 HTTP 交互修复、文字/图片/视频三类用户自有密钥迁移和网页中转现代化，并通过自动化与指挥线程浏览器验收。GitHub 功能交付已完成，生产尚未更新；不得把 GitHub 交付误写成生产部署完成，也不得把旧管理员共享密钥或账户端点方案重新当成当前任务。

任何新工作仍必须同时保护真实应用、用户数据、画布兼容性、积分结算、生成流程和恢复能力。

## 二、任何接手者必须先做什么

新的 AI 或开发者必须按照以下顺序开始：

1. 完整阅读 HANDOFF.md、GOALS.md、PROJECT_MEMORY.md、AGENTS.md、SPEC.md、PLAN.md、STATUS.md 和 DECISIONS.md。
2. 阅读 coordination 目录下所有 STATUS-*.md。
3. 运行 git status --short --branch 并检查 git diff。
4. 阅读与当前任务直接相关的实际代码。文档可能落后于代码。
5. 检查本地前端和 API 是否已经运行，不要重复占用端口。
6. 编辑前运行当前基线检查或相关的聚焦测试。
7. 说明准备修改的行为和文件，然后完成实现、验证与状态更新。

不要依赖历史对话重新推演项目。恢复项目状态时，以共享工作区、当前 Git 差异、测试结果和仓库中的记忆文件为准。

## 三、事实源优先级

当信息互相冲突时，按以下优先级判断：

1. 用户最新的明确要求。
2. 当前代码、数据库行为、浏览器状态和测试证据。
3. AGENTS.md 中的安全、端口和文件所有权规则。
4. DECISIONS.md 和 SPEC.md。
5. PLAN.md、STATUS.md 和 coordination/STATUS-*.md。
6. 本文中带日期的当前状态快照。
7. 历史对话和历史摘要。

状态文件写着“完成”不等于代码一定存在或可用。必须检查实际实现和验证证据。

## 四、用户偏好

以下偏好属于项目验收的一部分：

- 默认使用中文沟通，除非用户另有要求。
- 主动完成实现、验证和结果说明，不要只停留在建议或分析。
- 保护用户已经做过的工作。不得重置、检出覆盖、清理或删除无关的脏工作区修改。
- 执行破坏性或不可逆操作前，先准确解释影响。
- 参考图片必须一张一张检查和展示。绝不能一次输出八张参考图，大批量图片会让会话卡住。
- 不得把中转 API 密钥或其他秘密写入源码、文档、测试、截图、日志或线程消息。
- 发生画布版本冲突时，必须先保护本地草稿，再决定是否加载服务器版本。
- 进度和决策应写入仓库文件，使后续 AI 不依赖聊天记录也能恢复。
- 解释要具体，必须区分代码更新、服务器画布数据更新和浏览器本地草稿变化。
- 不要为了“看起来干净”而处理无关文件，也不要擅自改写用户已有内容。

## 五、项目的公共记忆与中枢大脑

本项目不存在一个隐藏且永久的 AI 大脑。项目公共记忆由共享仓库实现：

| 文件或证据 | 作用 |
| --- | --- |
| HANDOFF.md | 长期运行方式、架构、安全、恢复和用户偏好 |
| GOALS.md | 已确认执行事项的活动目标、步骤状态、分工、验收门禁和恢复入口 |
| PROJECT_MEMORY.md | 从历史项目线程提炼的用户目标、术语、偏好和待确认冲突 |
| AGENTS.md | 强制协作规则、端口、文件所有权和安全边界 |
| SPEC.md | 当前目标与验收标准 |
| PLAN.md | 当前任务拆分方式 |
| STATUS.md | 主集成线程的当前进度和下一步 |
| DECISIONS.md | 不应在没有新证据时反复推翻的决定 |
| coordination/STATUS-*.md | 各实施角色的进度、阻塞和验证证据 |
| Git diff 与测试 | 实际实现状态和客观证据 |

主 AI 线程是“中枢协调者”，负责跨模块决定、集成、最终浏览器验收和公共状态同步。实施角色只负责边界清晰的文件。
用户把主 AI 线程定义为“指挥官”。任何已经决定执行的想法都必须先登记为可恢复目标，再拆给边界清晰的协作线程；主线程负责检查实际差异和完成最终验收。

中枢共同研发协议：

1. 只拆分真正独立、可以并行的工作。
2. 每个实施角色拥有一个实现表面和一个状态文件。
3. 不允许两个角色同时编辑同一个实现文件。
4. 实施者工作时记录时间、完成内容、下一步、阻塞和验证证据。
5. 主线程检查实际差异，不仅相信实施者摘要。
6. 主线程负责跨文件集成、测试和浏览器验收。
7. 停止工作前，主线程同步 STATUS.md、相关角色状态和长期决定。
8. 只置顶当前指挥官主线程；协作线程和子智能体不要置顶。用户当前允许最多同时启用 3 个子智能体；不得超过这个上限，除非用户之后再次明确修改。只拆分真正独立的工作，不为凑数量创建线程，并服从平台实际提供的更小并发上限。
9. 子智能体默认使用 `gpt-5.6-sol` 与 `Ultra` 推理强度，除非用户当次明确覆盖。

2026-08-01 的交互与三用户密钥迁移获得了当轮明确并发例外，多个实施席按互不重叠的文件所有权并行工作。该例外随本地验收完成而结束；以后仍回到一次只激活一个实施者的默认规则，除非用户对新目标再次明确批准并发。

AI、线程和运行进程可能随时消失，文件会保留。任何宕机后仍需要知道的信息，都必须写进项目文件。

## 六、历史视觉文件所有权

已完成的视觉对齐阶段使用独立 CSS 覆盖层避免冲突：

| 角色 | 实现文件 | 状态文件 | 范围 |
| --- | --- | --- | --- |
| Shell | src/styles/canvas-shell-reference.css | coordination/STATUS-shell.md | 顶栏、资源面板、画布背景、菜单、响应式外壳 |
| Controls | src/styles/canvas-controls-reference.css | coordination/STATUS-controls.md | Dock、导航、缩放、小地图、外观浮层、移动端溢出 |
| Nodes | src/styles/canvas-nodes-reference.css | coordination/STATUS-nodes.md | 节点表面、标题、选中、连接点、缩放、工具栏和边线 |

该表仅用于解释现有文件来源，不是当前活动分工。新目标必须重新定义拥有文件；不要仅因看到这张表就激活旧线程或限制新任务范围。

三个视觉状态文件均已完成并归档。若未来继续修改这些表面，应创建新目标并更新相应状态，而不是把历史任务改回进行中。

## 七、系统架构

### 7.1 前端

- React 19、TypeScript、Vite 7。
- @xyflow/react 提供无限画布、节点、连接点、边线、小地图、缩放、选择和视口行为。
- lucide-react 提供界面图标。
- src/App.tsx 当前包含大部分应用和画布编排逻辑，是高冲突文件，修改必须克制。
- src/api.ts 管理浏览器 API 请求和会话行为。
- src/styles.css 是广泛的基础样式层。
- src/styles/canvas-*-reference.css 是当前视觉对齐覆盖层。
- src/main.tsx 依次导入 XYFlow 基础 CSS、基础样式和三个参考覆盖层。

### 7.2 后端

- Node.js ESM 和 Express 5。
- server/index.js 启动 HTTP 服务，并定期回收遗留的 AI 积分预占。
- server/app.js 包含认证、画布、资产、媒体、钱包、AI 中转、计费和管理员路由。
- server/db.js 包含 SQLite 表结构、增量迁移、数据库工具和遗留生成请求恢复。
- server/check-db.js 在备份或恢复前校验候选数据库和正式数据库。

### 7.3 数据层

- 默认数据库是 data/app.db。
- SQLite 存储用户、画布、资产、媒体、不可变积分流水、兑换码、核销记录、充值单、管理员审计、中转配置、生成请求和健康探针。
- 画布文档是 JSON，包含节点、边线和助手消息。
- 每张画布有 version 字段，用于乐观并发控制。
- 浏览器 localStorage 保存可恢复的本地草稿和部分 UI 偏好。
- 视频等媒体可以进入 media 表，并通过 /api/media/:id 提供。

### 7.4 中转与计费

- 服务器和管理员分别维护文字、图片、视频三个中转地址和开放模型列表；管理员界面与 `h` 不保存或展示共享运行时密钥。`AI_IMAGE_BASE_URL` 只作为图片地址的服务器默认/回退配置。
- 每位用户分别保存自己的文字、图片、视频 API 密钥。三类密钥均在服务端加密，浏览器只获得是否已配置和可选择的开放模型；账户安全不显示中转地址。
- 管理员测试三类中转、发现文字/图片/视频上游模型时，都使用当前管理员自己保存的对应用户密钥；每次重新发现会替换候选并清空旧勾选。
- AI 请求使用请求标识和 generations 记录实现幂等。
- 当前默认 `SITE_BILLING_ENABLED=0`：文字和视频不写站内预占、结算或退款流水；底层账本与显式开启兼容路径保留。图片生成在任何模式下都不扣站内积分，费用由用户自己的中转站账户处理。
- 文字调用优先使用 Responses 协议，解析 `output_text` 或嵌套输出文本；仅当上游返回 `404/405` 时回退 Chat Completions。

### 7.5 部署

- Dockerfile 构建前端并运行 Node 服务。
- docker-compose.yml 管理生产容器和持久化数据卷。
- deploy/nginx.conf 将公网流量代理到应用。
- deploy/install.sh 在 Ubuntu/Debian systemd 服务器上执行首次安装或安全快进更新：保留现有 `.env` 和数据卷，更新前备份运行中的数据库，并配置 Nginx/可选 HTTPS。
- deploy/install.sh 默认使用 0.0.0.0:3102 公网 IP+端口，不要求域名；提供域名时才安装/配置 Nginx，可选 Certbot HTTPS。
- 首次部署默认 `MOYU_ACCESS_MODE=public` 和 `PUBLIC_BIND=0.0.0.0`，自动探测并展示公网 IPv4。`h` 可切换 `public`、`domain`、`both`、`private`；旧部署没有模式字段时按现有域名和监听地址迁移。应用仅信任回环 Nginx 的转发头。
- 安装器会把 deploy/manage.sh 安装为 /usr/local/bin/h；sudo h 打开管理面板，sudo h status 输出模式、监听、地址和管理员登录信息。按用户明确要求，管理员密码在保留 bcrypt 登录哈希的同时以 JWT_SECRET 派生密钥做 AES-256-GCM 加密，仅由服务器 root 状态命令解密显示，应用 API 不返回；旧管理员需修改一次密码后才能同步显示。面板负责服务启停、安全快进更新、访问方式、端口、商业配额、备份恢复、日志、诊断和管理员初始化码轮换/清除；安全更新仍为第 5 项。
- `h` 不再提供文字、图片或视频中转入口。三类地址、上游模型发现和开放模型只在网页“运营管理”维护；旧共享密钥列仅保留为数据库兼容字段，Compose 不再把旧共享密钥环境变量注入应用容器。
- deploy/backup.sh 停止应用、复制数据目录、校验数据库并恢复服务健康。
- deploy/restore.sh 校验和迁移候选数据库、保留回滚快照、恢复后再次校验并检查健康。

## 八、运行方式

开发环境固定端点：

- 前端：http://127.0.0.1:5182/
- API 健康检查：http://127.0.0.1:3102/api/health
- 不得占用 3000 或 5174 端口。

Windows PowerShell 常用命令：

    npm.cmd ci
    npm.cmd run dev
    npm.cmd run build
    npm.cmd test
    git diff --check

npm.cmd run dev 会同时启动 Vite 和被监视的 API。启动前检查 5182 和 3102 是否已经运行。如果已有健康的服务，复用它，不要启动重复进程。

生产运行方式以 README.md、Dockerfile 和 docker-compose.yml 为准。默认公网 HTTP 未加密，不能用于长期传输登录密码或 API 密钥；完成首次部署后应通过 h 配置域名和 HTTPS。不得临时编造生产密钥，也不得公开 SQLite 数据目录。

## 九、环境变量与秘密

以 .env.example 为起点，但部署前必须同时检查当前代码，因为活跃开发期间示例可能暂时落后。

主要配置类别：

- 服务与数据库：PORT、DB_PATH、NODE_ENV。
- 安全：JWT_SECRET、ADMIN_SETUP_TOKEN。
- 账号限制：欢迎积分、画布数量、资产数量、文档大小、媒体大小和总存储限制。
- 文字中转：服务器地址、开放模型、token 价格、超时和响应大小；运行时认证来自当前用户的加密文字密钥。
- 图片生成：服务器地址/默认 `AI_IMAGE_BASE_URL`、开放模型、用户加密图片密钥和响应大小。
- 视频中转：服务器地址、开放模型、固定积分、超时、轮询、遗留回收和响应大小；运行时认证来自当前用户的加密视频密钥。
- 商业设置：每积分价格和人工充值说明。

强制规则：

- 永远不要提交 .env 或数据库文件。
- 调试时不得打印秘密值。
- JWT_SECRET 和 ADMIN_SETUP_TOKEN 必须使用不同的强随机值。
- 生产启动必须拒绝不安全密钥和不合法计费配置。
- 首位管理员创建后，按 README.md 说明处理初始化令牌。
- 即使为了临时调试，也不得把中转密钥发送到前端。
- `JWT_SECRET` 同时决定用户文字/图片/视频密钥、管理员可恢复密码密文和媒体签名能否恢复。当前没有自动轮换迁移；数据库备份必须与对应原值配套保存，严格恢复时由 `server/check-db.js` 验证三类用户密文和管理员密码密文可解密。
- `AI_VIDEO_MEDIA_ORIGINS` 只接受不带路径的精确 origin 列表，用于允许视频中转站之外的结果媒体来源。不得使用通配域名。

## 十、核心 API 分区

精确契约以 server/app.js 为准。目前路由分区包括：

- 健康和公共配置：/api/health、/api/config。
- 认证和账号：/api/auth/*、/api/me。
- 画布 CRUD：/api/canvases、/api/canvases/:id。
- 资产和媒体：/api/assets、/api/media/*。
- 钱包、兑换和充值：/api/wallet、/api/redeem、/api/topups。
- 用户密钥：`/api/me/text-key`、`/api/me/image-key`、`/api/me/video-key` 的保存、清除和测试。
- AI 中转：/api/ai/chat、/api/ai/image、/api/ai/video 和视频状态轮询；服务端验证开放模型并使用当前登录用户对应密钥。
- 管理员：总览、三类地址/模型配置及测试、文字模型发现、兑换码生成、充值通过和驳回。

所有用户数据的查询和修改必须在服务端执行所有权校验。前端隐藏按钮不等于权限控制。

## 十一、产品不变量

所有修改都必须保持以下行为：

- 用户只能访问自己的画布、资产、媒体、钱包和生成记录。
- 积分流水只追加不修改，余额更新和流水写入保持事务一致。
- 已成功的生成请求重放时不能重复扣费。
- 失败、过大、超时或遗留的中转请求必须退回预占积分。
- 三类用户中转密钥不能进入持久化浏览器状态、API 返回、日志、审计、生成记录或积分流水；上游返回值也不能反射当前用户密钥。
- 旧画布文档和本地草稿在修改后仍可加载。
- 画布版本冲突不能静默覆盖任何一方。
- 密码、登录锁定和会话撤销安全不能回退。
- 服务端配额不能通过直接调用 API 绕过。
- 健康检查需要证明 SQLite 可读写，不只是 HTTP 进程存活。
- 视觉修改后，图片工具和媒体操作必须继续工作。
- 视频结果下载必须逐跳校验精确 origin 和 DNS/IP，且只能向视频中转站自身 origin 发送认证头。
- 异步视频媒体配额 `413` 必须把任务标记失败并且只退款一次；结果下载型 `502/504` 保持 pending，允许后续轮询恢复。

涉及计费、认证、迁移、所有权或持久化时，应根据风险增加或扩展测试。

## 十二、画布草稿与版本冲突安全

这是最容易被误解的地方，必须准确解释。

页面提示“服务器存在更新”时，表示同一张画布在其他页面、设备或会话中发生了版本变化。它不是 Git 更新，也不是应用程序升级。

冲突处理逻辑：

- 浏览器恢复本地草稿。
- 自动保存被阻止，因此本地草稿不会静默覆盖较新的服务器画布。
- 顶部警告提供下载草稿、导入草稿和使用服务器版本操作。
- 点击“使用服务器版本”会再次弹出确认。
- 确认后会删除该画布在浏览器中的本地草稿，并重新加载服务器版本。
- 这不会删除源码、Git 修改、账号、积分或其他画布。

安全操作顺序：

1. 如果本地内容可能有价值，先取消确认弹窗。
2. 使用警告条上的下载按钮导出本地草稿 JSON。
3. 确认下载文件存在且非空。
4. 确实想放弃本地内容时，才选择服务器版本。
5. 想保留两个版本时，把下载的草稿导入另一张新画布，或者进行有意识的合并。

导入草稿会在确认后替换当前画布内容，也属于破坏性操作，必须先备份。

## 十三、视觉对齐目标

参考材料：

- 参考仓库：C:/Users/Administrator/AppData/Local/Temp/basketikun-infinite-canvas-review
- 当前截图目录：C:/Users/Administrator/AppData/Local/Temp/infinite-canvas-reference-8
- 截图文件：01.png 到 08.png

参考仓库使用 AGPL-3.0。把它作为视觉和行为基准，不要盲目复制源码。上述路径属于本机临时路径，换机器或长期任务中可能消失。

用户指定的图片工作规则：

1. 检查一张参考图。
2. 完成或验证与该图相关的修改。
3. 再检查下一张。
4. 不允许一次加载、展示或输出八张图片。

视觉验收目标：

- 顶栏轻盈、悬浮，并保持约 64px 的节奏。
- 左侧资源面板遵循约 300px 的参考节奏，不创建第二条永久应用侧栏。
- 底部 Dock 是尺寸稳定的 56px 紧凑图标工具面，并有工具提示。
- 左下导航是紧凑的 56px 控制组。
- 画布背景、网格、节点表面、悬停、选中、连接点、曲线和缩放手柄接近参考比例与配色。
- 桌面端和移动端不能出现文字、面板、节点工具栏或画布控件重叠。
- 账号、积分、中转、草稿、生成和图片工具不能回退。

## 十四、当前状态快照

当前分支为 `codex/infinite-canvas`，并跟踪 `origin/codex/infinite-canvas`。网页中转现代化功能提交 `99926be16bf561cfdb1ddfe4d51eb6ce4d7936e4` 已正常推送；随后仅补充交付状态文档。生产仍停留在最后单独核实的 `3f7b52d`，本轮改动尚未部署。

当前本地候选已经实现：

- 普通 HTTP 不再依赖安全上下文 API；即使页面级 `crypto` 对象不可用，画布仍可创建、保存、刷新和恢复。
- 汉堡命令菜单与“我的画布”侧栏职责分离，侧栏可用关闭按钮、遮罩和 `Escape` 收回；所有 Dock、导航与外观操作均保持页面可用。
- 用户分别保存文字、图片、视频三类加密密钥；运行时按登录用户和媒体类型取对应密钥。
- 账户安全仅显示三类密钥控件和配置状态，不显示中转端点，也不返回密钥材料。
- 运营管理包含文字、图片、视频三个地址/开放模型标签，无共享密钥控件；管理员测试使用自己的对应用户密钥。
- 文字、图片、视频上游模型发现都使用管理员保存的对应用户密钥；重新获取会替换候选并清空旧勾选，生成节点只使用管理员明确保存的开放模型。
- 文字优先调用 Responses，仅 `404/405` 回退 Chat Completions；新部署不预设文字或图片模型。
- 站内计费默认关闭，文字/视频生成写入零预占与零扣费状态，不产生账本流水；显式开启兼容路径仍有自动化保护，图片始终为零站内积分。
- 右上角原积分入口改为“API 密钥”；余额、钱包、兑换码、充值申请/记录和充值审核不在当前网页界面显示。
- `h` 删除三类中转配置，仍保留第 5 项安全更新；中转地址、模型发现和开放模型只在网页运营管理完成。
- 画布标题可单击/触屏进入编辑，Enter 直接完成保存，刷新后仍保留新名称。
- 上游错误、内容、usage、模型、图片结果和视频任务字段均有当前用户密钥反射防护，不能把密钥写入响应、日志或持久化记录。

最终本地验证证据：

- 服务端 `19/19`、生产配置 `12/12`、relay modernization `4/4`、web contract `4/4`、UUID `1/1`，完整套件 `40/40`。
- `npm.cmd run build` 通过，产物为 `index-CuruJLMo.js` 和 `index-CUxjUyFM.css`；四个 Node 与四个 Shell 语法检查、凭据扫描和 `git diff --check` 通过，仅有既有换行提示。
- 指挥线程在隔离普通 HTTP 实例完成桌面 `1440x1000` 和移动 `390x844` 验收，覆盖三密钥、三类模型发现/保存/选择、Responses 文字、旧勾选清空、API 密钥入口、零计费界面、画布重命名持久化、Dock、侧栏、console、网络和溢出。
- 浏览器 warning/error、页面错误、失败请求、HTTP 错误和外部资源请求均为零；根页面、健康接口及构建 JS/CSS 均返回 HTTP 200。
- 临时 `3123/3124` 进程、模拟上游、数据库和日志均已删除，浏览器视口已重置，隔离标签页已关闭；真实 `3102/5182` 未被操作。

真实浏览器仍可能显示画布版本冲突。没有下载本地草稿前，不得选择放弃本地草稿。本轮验收使用隔离实例，未打开或修改真实 `5182` 草稿。

## 十五、立即下一步

下一位 AI 应从当前实现继续，不要重新分析历史对话：

1. 读取 `GOALS.md`、`STATUS.md`、`coordination/STATUS-*.md` 和当前 Git 状态，不要重新分析历史对话。
2. GitHub 交付已完成。由用户自己在 MobaXterm 的 `h` 面板输入 `5` 更新；主线程不控制 SSH。
3. 更新后核对部署 SHA、更新前自动备份、数据库、健康接口、公共页面和本轮核心工作流。
4. 生产接受前保留 `3f7b52d` 为当前生产 SHA，不得声称本轮已部署。独立的 `api.bkbk.baby` DNS/Beaver 80/443 冲突仍需可逆备份和明确批准。
5. 新目标默认一次只激活一个 `gpt-5.6-sol`、`Ultra` 子智能体；即使用户批准并发，同时也不得超过 3 个。

## 十六、浏览器验收清单

localhost 工作优先使用内置浏览器。

桌面端：

- 页面可渲染，画布非空，节点和网格可见。
- 顶栏不覆盖标题、助手、侧面板或菜单。
- 侧面板打开时正确改变画布可用宽度。
- Dock、导航、小地图和外观浮层可访问。
- 节点工具栏不撑大节点且不超出视口。
- 曲线和连接点有足够对比度。
- 右键菜单和对话框保持在视口内。

移动端：

- 顶部操作不覆盖画布标题。
- 侧面板位于顶栏下方、Dock 上方。
- 节点工具栏可固定或横向滚动，不覆盖无关控件。
- Dock 在必要时可以横向滚动。
- 导航仍可使用，小地图按设计隐藏。
- 按钮或控件中的文字不被裁剪。

控制台和网络：

- 没有新增运行时异常。
- 没有由修改引起的重复失败 API 请求。
- 日志和响应中没有秘密。
- 没有异常保存循环或重复扣费。

## 十七、验证门禁

宣布实现完成前必须运行：

    npm.cmd run build
    npm.cmd test
    git diff --check

后端或数据库修改还要运行相关聚焦测试，并对目标数据库验证 /api/health。

视觉修改仅通过静态检查还不够，必须完成桌面和移动浏览器检查以及控制台检查。

部署或迁移修改必须先验证数据库副本。使用现有备份与恢复流程，不得临时手工替换正式数据库。

## 十八、标准改动流程

普通任务使用以下流程：

1. 定义可观察的完成结果。
2. 找到最小的拥有文件。
3. 在相关状态文件中记录开始。
4. 阅读已有模式，不随意增加抽象。
5. 做范围克制的修改。
6. 立即运行聚焦验证。
7. 共享契约有风险时扩大邻近测试。
8. 检查 Git 差异，寻找意外改动和秘密泄漏。
9. 更新状态和长期决定。
10. 报告修改、验证和剩余风险。

不要仅因为 src/App.tsx 很大就顺手重构。它是已知维护风险，但无关拆分会增加冲突和回归。只有真实任务需要且有测试保护边界时才提取模块。

## 十九、维护手册

### 19.1 修改计费或中转

- 验证幂等、预占、结算、退款和遗留回收。
- 测试成功、上游错误、超时、响应过大、重试和缺失 usage 等路径。
- 确认文字、图片、视频分别使用服务器当前地址/开放模型与登录用户对应的加密密钥；管理员测试必须使用管理员自己的对应用户密钥。
- 默认关闭计费时确认文字/视频零预占、零账本流水；显式开启时验证既有预占、结算、退款与恢复仍完整；图片始终零站内积分。所有模式都必须验证上游内容不能把当前用户密钥反射到响应、日志或持久化记录。

### 19.2 修改数据库

- 优先在 server/db.js 中使用可重复执行的增量迁移。
- 测试从旧数据库结构迁移且不丢行。
- 正式必需结构变化时同步 server/check-db.js。
- 验证 quick_check 和 foreign_key_check。
- 没有验证过的副本或备份时，不得在 data/app.db 上试验。

### 19.3 修改画布持久化

- 保持文档兼容和节点默认值。
- 测试版本冲突与本地草稿恢复。
- 不得静默选择本地或服务器内容。
- 验证切换画布时会保存，或者安全阻止切换。

### 19.4 修改视觉

- 优先修改对应范围的参考 CSS 层。
- 检查明亮和深色主题。
- 检查面板打开和关闭、选中和未选中状态。
- 桌面和移动端分开验证。
- 一次只处理一张参考图。

### 19.5 生产事故

- 先保护证据和数据，不删除日志或数据库文件。
- 检查进程或容器，以及 /api/health。
- 检查磁盘空间和数据卷是否可用。
- 使用 server/check-db.js 校验数据库副本。
- 积分预占卡住时检查 generations 状态并使用现有恢复逻辑，不得手工改余额。
- 只从验证过的备份恢复，健康确认前保留回滚快照。
- 把事故、命令、受影响版本和结果写入持久文档，但不得写秘密。

## 二十、已知风险与缺口

- 脏工作区包含大量未提交的重要改动，破坏性 Git 命令影响非常大。
- src/App.tsx 和 server/app.js 是大型共享热点，并行编辑必须严格分配所有权。
- 自动化测试无法替代视觉截图和控制台检查。
- 状态文档可能落后几分钟，必须检查代码和最新测试输出。
- 浏览器本地草稿不在 Git 内，清理浏览器存储或冲突操作可能导致丢失。
- 参考仓库和截图目录属于本机临时路径。
- 活跃开发期间环境示例可能落后，部署前要比较 .env.example、README 和代码。
- 当前记录的完整测试为 40/40：服务端 19/19、生产配置 12/12、relay modernization 4/4、web contract 4/4、UUID 1/1；扩展高风险路径时仍需增加对应测试，不能用另一类媒体测试替代。
- 视频下载在请求前检查 DNS/IP，但 DNS 校验和实际连接之间仍存在 rebinding TOCTOU 窗口。需要进一步收紧时，应使用能够把已验证地址绑定到实际连接的网络层实现。
- 当前 Git 运行时自带 Bash；`deploy/install.sh`、`deploy/manage.sh`、`deploy/backup.sh` 和 `deploy/restore.sh` 均已通过 `bash -n`。本机没有 Docker，因此一键安装仍需在实际 Ubuntu/Debian systemd 服务器完成 Docker Compose、Nginx、Certbot、UFW 和容器秘密清除实跑。

## 二十一、完成定义

只有同时满足以下条件，任务才算完成：

- 请求的行为在正确的所有权边界内实现。
- 用户已有工作和无关脏修改被完整保留。
- 相关自动检查通过。
- 需要的浏览器或运行验证已经完成。
- 没有泄露秘密或用户数据。
- 状态文件准确记录完成内容、下一步、阻塞和证据。
- 新的长期决定已写入 DECISIONS.md。
- 最终说明明确区分已完成工作和剩余风险。

上下文不足、AI 宕机、线程消失或进程重启都不等于完成。停止前应写下精确检查点，使下一位 AI 能安全继续。

## 二十二、历史交接点：公网端口与 h 面板（2026-07-31）

本节保留 `7c48c59` 交付当时的恢复背景，不是当前三密钥合同或最新测试状态。当前接班必须先读取本文第十四、十五节、`GOALS.md`、`STATUS.md`、`DECISIONS.md` 与 `git status`。

- `docker-compose.yml` 默认发布 `${PUBLIC_BIND:-0.0.0.0}:${PUBLIC_PORT:-3102}:3102`；`.env.example` 提供 `PUBLIC_BIND=0.0.0.0` 和 `PUBLIC_PORT=3102`。
- `deploy/install.sh` 默认不要求域名，支持 `--port` 和 `--bind`，保留已有 `.env`/数据，拒绝脏仓库和非快进更新，更新前校验备份，并对代码、环境、应用和已捕获 Nginx 状态执行失败回滚。首次域名/TLS 失败回退公网 HTTP 时会明确警告未加密；自动恢复失败会保留 `0600` 救援快照。
- 当时的 `deploy/manage.sh` 还负责共享文字/视频密钥迁移；该行为先被 2026-08-01 三用户密钥合同取代，随后 2026-08-02 又删除了 `h` 的三类中转入口。当前中转地址、发现与开放模型只在网页运营管理维护。
- `deploy/backup.sh` 与 `deploy/restore.sh` 从 `.env` 读取并校验 `PUBLIC_PORT`，健康检查始终访问 `127.0.0.1:<PUBLIC_PORT>`。
- 当时验证：`npm.cmd run build`、`npm.cmd test`（23/23）、生产配置测试（9/9）、Shell/Node 语法、diff、秘密扫描和本地 `3102/5182` 健康检查通过。最新证据以第十四节和 `STATUS.md` 为准。
- 不要提交 `.env`、`data/`、Docker 数据卷、备份、日志、`dist/` 或任何真实 API 密钥。图片参考仍一次只处理一张。
