# 无限画布项目交接文档

> 项目名：墨屿画布（Infinite Canvas）
> 用途：供完全没有旧聊天上下文的新 Codex 会话接管项目。
> 最后同步：2026-08-02 23:00，Asia/Shanghai。
> 这份文件是接班入口，不是代码事实的替代品；实际工作树、代码、数据库契约和新鲜测试结果始终优先。

## 一、新会话接管协议

当用户说“你去看一下项目的交接文档”“现在这个项目由你来接管”或含义相近的话时，把它视为明确的项目接班指令。

### 1.1 不要恢复旧聊天

- 不读取、分叉、恢复或依赖任何旧会话 ID。
- 不要求用户重新讲述历史。项目记忆已经写入仓库。
- 旧聊天摘要只能作为最低优先级线索，不能覆盖当前工作树和用户最新指令。

### 1.2 默认使用 Codex 目标模式

用户明确偏好用 Codex 目标模式执行项目任务。接班本身也应作为一个目标执行。

1. 先检查当前目标状态。
2. 如果没有未完成目标，创建“项目接班检查”目标；用户的接管表述就是创建该目标的明确授权。
3. 目标应包含可验证结果：读完恢复文档、核对 Git、核对实际代码/测试/端口、报告当前状态和阻塞。
4. 对后续非简单任务，在编辑前建立或恢复对应目标，并维护可恢复计划。
5. 只有实现、测试、界面验收、状态同步和临时资源清理全部完成后，才能把目标标记为完成。
6. 不因为上下文将满、任务很难或时间较长而虚假完成目标。
7. 除非用户明确给出 token 预算，否则不要给目标设置 token 预算。

建议的接班目标：

```text
完成无限画布项目接班检查：读取项目恢复文档，核对当前 Git、实际代码、测试与运行服务，确认安全边界、用户偏好、当前交付状态和下一项用户指令。
```

### 1.3 首次接班的读取顺序

先遵守 `AGENTS.md`，然后按以下顺序完整读取：

1. `HANDOFF.md`
2. `GOALS.md`
3. `PROJECT_MEMORY.md`
4. `SPEC.md`
5. `PLAN.md`
6. `STATUS.md`
7. `DECISIONS.md`
8. `coordination/STATUS-*.md` 中与当前目标有关的文件；接班审计时应至少浏览全部文件的状态和最后记录

之后只读检查：

```powershell
git status --short --branch
git diff
git log -5 --oneline --decorate
git rev-parse HEAD
git rev-parse origin/codex/infinite-canvas
```

再检查：

- 与当前任务直接相关的实际代码。
- 当前测试基线或聚焦测试。
- `3102`、`5182` 是否已有健康进程。
- 文档所写状态是否与代码、Git 和测试证据一致。

如果用户只说“接管项目”，首回合只完成接班检查，不开始实现、不看图片、不启动浏览器或重复开发服务器。简洁报告分支、HEAD、工作树、最近交付、生产边界和当前目标，然后等待具体任务。

如果用户在同一条消息里同时给出了明确实现任务，先完成上述快速接班审计，再为该任务建立目标并继续执行；不得跳过 Git 和安全边界检查。

### 1.4 持续交接检查点协议（强制）

所有目标都必须维护可落盘、可独立恢复的持续交接检查点。聊天消息、目标工具状态和内存中的计划不能替代仓库检查点；新的会话必须能够不依赖上一段聊天直接恢复。

必须写检查点的时机：

1. 每个目标完成后，在标记目标完成和发送最终回复前同步最终检查点。没有最终检查点，目标不得宣称完成。
2. 预计当前任务、下一阶段或下一组验证可能超过当前上下文时，必须在继续前先写中途检查点，不要等到上下文已经耗尽。
3. 对是否能在当前上下文内完成存在合理疑问时，默认先写检查点。
4. 用户暂停、方向改变、会话准备切换或外部阻塞出现时，只要目标仍未完成，也应先写中途检查点。

落盘位置：

- 当前目标的完整实时检查点写入对应的 `coordination/STATUS-<goal>.md`。
- 同步更新 `GOALS.md` 中的目标状态、`PLAN.md` 中的步骤状态和 `STATUS.md` 中的当前摘要。
- 当前权威检查点、长期偏好或长期决定发生变化时，再同步 `HANDOFF.md`、`PROJECT_MEMORY.md` 或 `DECISIONS.md`。
- 中途检查点必须保持目标为 `active`，准确写清未完成项；不得为了换会话而虚假完成目标。

每个检查点固定包含以下字段，不得省略：

```markdown
## Checkpoint: YYYY-MM-DD HH:mm +08:00

- 当前目标：一句话写明正在实现或验证什么。
- 已完成：已经落盘并可由 Git/文件/测试证实的工作。
- 未完成：仍需实施、验证、清理、提交、推送或部署的事项。
- 最后验证结果：最后运行的命令、通过数量、失败项和未运行的门禁。
- 修改文件：本目标已修改和新增的准确路径；说明是否包含接手前已有修改。
- 下一步：恢复后首先执行的一个具体动作，再说明后续顺序。
- 恢复命令：可直接运行的工作目录、Git/差异检查、必读文件和聚焦验证命令。
```

恢复命令必须具体、可复制执行，并先只读核对状态。例如：

```powershell
Set-Location 'C:\Users\Administrator\Documents\无限画布'
Get-Content -Encoding UTF8 HANDOFF.md
Get-Content -Encoding UTF8 GOALS.md
Get-Content -Encoding UTF8 STATUS.md
Get-Content -Encoding UTF8 coordination/STATUS-<goal>.md
git status --short --branch
git diff -- <本目标修改文件>
<本目标最后一个聚焦验证命令>
```

恢复命令不得包含密钥、密码、认证头、真实数据库内容、破坏性 Git 命令或未经确认的数据清理。最终检查点还必须记录全部验收门禁和临时资源清理结果。

## 二、当前权威检查点

以下是写本文时的已验证状态。新会话仍必须重新运行 `git status`，因为本文更新本身可能形成新的未提交差异。

- 分支：`codex/infinite-canvas`。
- AI 运行时产品、交接文档和最终交付检查点均已推送到 `origin/codex/infinite-canvas`，并在 fetch 后核对远端完整 SHA。
- 已交付产品 HEAD：`90e091aab59bdbeaf8f3b8edcdc2e28bd06c8098`。
- 最新功能提交：`90e091a`，`fix: repair AI text runtime and IME input`。
- 主要交接文档提交：`44dd2441a9df836277905fdf784aeda0ac725c32`，`docs: refresh project handoff and runtime delivery`；其后还有本轮最终检查点提交，以实际 `git rev-parse HEAD` 为准。
- 最近完整自动化：`npm.cmd test` 通过 `58/58`。
- 最近生产构建：`index-Bo6TNF-x.css`、`index-Bcuyr84L.js`。
- 最近 `git diff --check` 通过，仅有 Windows LF/CRLF 转换提示。
- 本地前端已在 `http://127.0.0.1:5182/` 运行。
- 本地 API 已在 `http://127.0.0.1:3102/api/health` 运行。
- 写本文时 `3000`、`5174` 没有被本项目占用；不得使用这两个端口。
- AI 运行时产品修复 `90e091a`、主要交接文档 `44dd244` 和最终交付检查点均已正常推送，并经 fetch 与 `ls-remote` 核对完整 SHA；GitHub 交付没有剩余事项，生产仍未更新。
- 最后单独核实的生产 SHA 仍是 `3f7b52d`。在完成一次新的生产更新和验收前，不得声称生产已包含后续 GitHub 功能。
- 当前没有待分析的用户原始截图。
- 本轮交接文档更新是文档任务，不代表新的产品功能或生产部署。

## 三、最近完成的产品修复

### 3.1 AI 运行时修复（产品与交接文档均已推送）

当前工作树修复了三个关联问题：

1. AI 文字模式始终显示连接图片数量和最多四张参考图预览；生成请求把这些图片转换为经过服务端校验的多模态引用，不再只发送文字提示词。
2. 文字中转的 Responses 和 Chat Completions 请求都显式发送 `stream: false`。Responses 仍优先，仅在上游 `404/405` 时回退；两种协议分别使用兼容的文字/图片内容块。
3. 画布助手 textarea 在 IME composition 期间由 DOM 持有正在组合的值，结束后才同步 React 状态，避免受控回写造成中文标点重复；composition、`nativeEvent.isComposing` 或 Windows `keyCode 229` 期间不会把 Enter 当作发送。普通 Enter 只发送一次，Shift+Enter 保持换行语义，且不使用会吞掉合法双标点的字符去重。

隔离验收使用合成 1x1 data URL、虚构账号/密钥和回环中转：文字模式显示 `0 段文本 · 1 张图片` 与 `1 张参考图`，生成结果为 `AI 已识别 1 张参考图`，中转摘要记录 `stream:false`、`imageCount:1`；最终输入实现中单个 `，` 长度为 1，主动输入 `，，` 长度为 2，普通 Enter 只产生一问一答和一次中转调用。完整测试 `58/58`、build、语法和差异检查通过，浏览器 warning/error 为空。隔离浏览器不能完整模拟 Windows 中文输入法候选组合界面，因此真实 OS IME 是部署后现场复核项。端口 `3143/3144`、临时数据库/日志/脚本和标签已清理；真实 `3102/5182` 未被浏览器打开且仍健康。完整证据见 `coordination/STATUS-ai-runtime.md`。

### 3.2 AI 可用性交付（已推送）

交付 `7c774c1` 修复了以下用户可见问题：

1. 节点左右连接点改为始终可见的 `14px` 圆形标记。左侧输入点为青绿色，右侧输出点为蓝色，深浅主题、连接、有效和焦点状态都有高对比表现。
2. 用户输入新的文字、图片或视频密钥并点击测试时，流程改为“先测试，成功后保存，再刷新已配置状态”；测试失败不会保存。测试已保存密钥仍是纯测试操作。
3. 手工连接图片/文本与 AI 节点时，不再依赖拖线方向。`图片 -> AI` 和 `AI -> 图片` 都可作为上下文；自动生成结果边被标记为 `result`，不会重新流入后续生成上下文。
4. 管理员没有开放对应模型时，生成按钮不再像死按钮一样静默失效，而会明确提示“管理员尚未开放生图/文字/视频模型”。

隔离浏览器证据：

- 反向 `AI -> image` 手工连接显示 `0 段文本 · 1 张图片` 和 `1 张参考图`。
- 图片生成实际调用 multipart `POST /v1/images/edits`，包含 `image` 字段。
- 生成记录成功且站内扣费为零，画布新增 `图片结果 · image-test` 节点。
- 空图片模型列表会显示 `管理员尚未开放生图模型`，不会再调用中转。
- 桌面、`390x844` 移动端、深色和浅色主题通过；页面无横向溢出，浏览器 warning/error 为空。
- 隔离端口、临时数据库、虚构账号/密钥和浏览器标签已清理；真实 `3102/5182` 未被替换。

相关入口：

- `src/App.tsx`：账户密钥、画布编排、边角色、生成流程和主要 UI。
- `src/generation-context.ts`：类型化文本/图片上下文遍历。
- `src/styles/canvas-nodes-reference.css`：节点、连接点、工具栏和边线最终视觉覆盖。
- `tests/ai-creation-contract.test.mjs`：AI 创作、上下文、密钥语义和连接点契约。
- `coordination/STATUS-ai-usability.md`：该轮完整验收记录。

## 四、用户协作偏好

这些不是装饰性偏好，而是项目执行方式的一部分。

### 4.1 沟通与执行

- 默认使用中文。
- 语气直接、具体，说明正在做什么、为什么，以及验证结果。
- 用户希望 AI 主动把任务做到实现、验证和收尾，不只给建议、分析或半成品。
- 开始长任务后保持推进；遇到阻塞先自己调查，不要过早把问题交还用户。
- 重要进度写进仓库状态文件，使下一次会话不依赖聊天记录。
- 解释时明确区分：代码/Git 更新、服务器画布数据、浏览器本地草稿、GitHub 交付、生产部署。
- 不要为了“看起来干净”而改动无关文件、清理用户修改或顺手重构大文件。
- 用户说“推送”时，检查、提交并正常推送本轮已验收范围，随后核对远端 SHA；禁止强推。
- GitHub 推送不等于生产部署。只有用户明确要求部署时，才能建立独立部署目标。

### 4.2 目标模式

- 默认把非简单执行任务放进 Codex 目标模式。
- 用户说“开始以目标的形式执行”“继续这个目标”或“接管项目”，不要只写计划，要调用目标工具并持续更新。
- 目标必须有可观察成功标准、当前步骤、阻塞和验证证据。
- 如果已有未完成目标，优先恢复它，不要静默覆盖。
- 如果用户改变方向，更新当前目标/计划，使最新指令成为事实源。

### 4.3 图片与截图

- 每个回合最多读取一张用户提供的原始图片，最多调用一次图片查看工具。
- 禁止同回合读取第二张图片，禁止批量或并行看图。
- 禁止创建或读取裁剪图、拼接图、放大图、缩略图等衍生图片。
- 完成唯一原图分析后结束该回合，不在同一回合实现、启动浏览器或看下一张。
- 这是上下文容量限制，不是逐图授权。目标仍有待处理原图时，后续新回合可以自动处理下一张，不需要用户重复说“看下一张”。
- 用户明确暂停、改变目标或图片已处理完时才停止。

### 4.4 Worker 与线程

- 默认一次只激活一个 worker/subagent。
- 只有用户对当前目标明确批准并发时才并发。
- 同时最多 3 个 worker/subagent；平台更小的并发限制优先。
- 不为凑数量创建 worker。
- 并发前必须定义不重叠的文件所有权。
- 只置顶当前指挥官主线程；协作线程保持不置顶。
- 历史 worker 所有权只是归档，不是新任务的站立授权。

## 五、事实源与文件职责

信息冲突时按以下优先级处理：

1. 用户最新明确要求。
2. 当前工作树、实际代码、当前数据库/API 行为、浏览器状态和新鲜测试。
3. `AGENTS.md`。
4. `DECISIONS.md` 和 `SPEC.md`。
5. `GOALS.md`、`PLAN.md`、`STATUS.md`、`coordination/STATUS-*.md`。
6. 本文的日期快照。
7. 历史聊天或历史摘要。

文件分工：

| 文件 | 作用 |
| --- | --- |
| `AGENTS.md` | 强制执行规则、端口、安全、并发与验证 |
| `HANDOFF.md` | 新会话接班入口、当前快照、偏好和踩坑 |
| `GOALS.md` | 可恢复目标账本和历史验收 |
| `PROJECT_MEMORY.md` | 用户术语、长期偏好和产品记忆 |
| `SPEC.md` | 当前接受的产品基线 |
| `PLAN.md` | 当前/最近任务步骤 |
| `STATUS.md` | 主线程最新状态、证据和剩余风险 |
| `DECISIONS.md` | 已决定且不应反复推翻的技术/协作选择 |
| `coordination/STATUS-*.md` | 各轮实现与验收的详细证据 |
| Git diff、测试、浏览器 | 实际事实 |

## 六、架构与数据归属

### 6.1 前端

- React 19、TypeScript、Vite 7。
- `@xyflow/react` 提供画布、节点、边、连接点、视口和缩放。
- `lucide-react` 提供图标。
- `src/App.tsx` 是大型共享热点，包含大量应用与画布编排逻辑；按任务做最小修改，不要顺手重构。
- `src/api.ts` 管理浏览器 API 和会话。
- `src/styles.css` 是基础样式。
- `src/styles/canvas-shell-reference.css`、`canvas-controls-reference.css`、`canvas-nodes-reference.css` 是当前视觉覆盖层。

### 6.2 后端

- Node.js ESM、Express 5、SQLite `node:sqlite`。
- `server/app.js`：认证、用户、画布、资产、媒体、AI 中转、管理员和计费路由。
- `server/db.js`：表结构、增量迁移、数据库工具和生成恢复。
- `server/check-db.js`：备份/恢复数据库的严格校验。
- `server/index.js`：HTTP 启动与遗留生成恢复定时器。

### 6.3 正式资产在哪里

正式资产以服务器为准，不是只放在用户浏览器本地：

- 画布节点、边和助手消息保存在 SQLite `canvases.document` JSON。
- 资产库图片/文本等保存在 SQLite `assets` 表。
- 视频媒体保存在 SQLite `media` 表的 BLOB，并通过 `/api/media/:id` 提供。
- 压缩后的 `data:image/...` 图片可能直接进入画布 JSON 或资产 `content`。
- 上游返回远程 URL 时，当前画布/资产可能保存该 URL；原始字节仍由对应远程地址托管，除非另行转存。
- 浏览器 `localStorage` 只保存未同步草稿、AI 请求幂等键和界面偏好，不是正式资产库。

### 6.4 密钥与中转

- 文字、图片、视频运行时认证来自当前登录用户自己保存的对应加密密钥。
- 管理员只管理三类中转地址和开放模型，不管理共享运行时密钥。
- 账户安全只有三个用户密钥控件，不显示中转地址，不回显密钥。
- 管理员测试和模型发现使用管理员本人保存的对应用户密钥。
- 旧共享密钥列仅用于数据库兼容，不得重新启用。
- 文字优先使用 Responses，仅上游 `404/405` 时回退 Chat Completions；两种协议都显式非流式。AI 文字节点可把最多四张连接图片作为类型化多模态引用发送，图片数据不进入可见提示词。
- 当前默认 `SITE_BILLING_ENABLED=0`；文字/视频站内扣费为零，图片始终为零站内积分。

## 七、不可违反的安全边界

- 永远不要把用户文字、图片或视频中转密钥、管理员密码、密文、Authorization 头或可恢复秘密片段写进源码、文档、测试、截图、日志、命令输出或线程消息。
- 不提交 `.env`、`data/`、数据库、WAL/SHM、Docker 数据卷、备份、日志或 `dist/`。
- 不读取真实 `.env` 或真实数据库内容来完成普通前端/测试任务。
- 不删除或覆盖用户画布、浏览器草稿、数据库、Docker 卷或 `/srv/canvas-backups`。
- 不使用 `git reset --hard`、`git checkout --`、`git clean` 或其他破坏性 Git 命令清理用户工作。
- 画布版本冲突时先保护本地草稿。没有导出草稿前，不点击“使用服务器版本”。
- 用户数据所有权必须由服务端验证，不能依赖前端隐藏按钮。
- 上游错误、模型、usage、图片、视频任务和缓存结果都不能反射当前用户密钥。
- `JWT_SECRET` 是加密数据库恢复身份的一部分；数据库备份必须配套保留原值，不能随意轮换。

## 八、项目踩过的坑

### 8.1 文档会落后，甚至标错“Active”

- `GOALS.md` 和历史状态包含大量已完成轮次，有些旧标题仍写着 Active。
- 必须看状态字段、最新时间、实际 Git 和代码，不能只看标题。
- 当前权威基线是本文件第二节和实际 HEAD，不是较早的 `40/40`、`50/50` 或旧构建产物。

### 8.2 PowerShell 中文乱码

- Windows 默认 `Get-Content` 可能把 UTF-8 中文显示成乱码。
- 读取中文文档应使用：

```powershell
Get-Content -Raw -Encoding UTF8 HANDOFF.md
```

- 不要把乱码内容写回文件。
- 手工编辑必须用 `apply_patch`；不要用错误编码的 PowerShell 管道重写中文文档。

### 8.3 GitHub 直连可能失败

- 本机曾出现 `github.com:443` 直连超时或 `Empty reply from server`。
- Windows 系统代理当时为 `127.0.0.1:7891`，进程为 Clash/Mihomo。该端口不是永久保证，使用前先检查监听和 HTTPS 连通性。
- 只对当前命令临时使用代理，不写永久 Git 配置：

```powershell
git -c http.proxy=http://127.0.0.1:7891 ls-remote origin refs/heads/codex/infinite-canvas
git -c http.proxy=http://127.0.0.1:7891 push origin codex/infinite-canvas
```

- 推送后 fetch 并核对本地/远端完整 SHA。
- 禁止强推。

### 8.4 浏览器自动化可能“点击了”覆盖层

- 在 `390x844` 视口，打开的左侧画布面板曾覆盖画布中的生成按钮。定位器找到了按钮，但坐标命中的是覆盖面板，因此没有触发生成。
- 交互前检查最新 DOM、唯一性和实际命中层；必要时用只读 `elementFromPoint` 验证按钮中心。
- 切换桌面视口或收起面板后再测，不要把覆盖层问题误判为 AI 回调失败。
- 验收结束必须 reset 临时 viewport 并关闭隔离标签页。

### 8.5 生成幂等缓存会重放旧结果

- AI 请求键保存在浏览器 `localStorage`，服务端结果保存在 `generations`。
- 相同画布、节点和请求内容可能命中旧成功结果，不会再次调用假中转。
- 隔离测试需要全新请求内容，或只在确认过的临时数据库/浏览器中清理对应幂等数据。
- 禁止为了解除测试缓存清理真实浏览器 localStorage 或真实 `generations`。

### 8.6 测试图片夹具不一定能被浏览器解码

- 极小 base64 PNG 曾让服务端成功，但浏览器 `createImageBitmap`/压缩阶段失败，导致没有结果节点。
- 隔离假中转可返回本地受控的有效图片 URL，再从网络请求、数据库状态和结果节点三方核对。
- 这不授权读取用户原图，也不要创建裁剪、拼接或放大衍生图。

### 8.7 Node SQLite 与命令参数

- 本项目不保证安装 `better-sqlite3`；当前服务器使用 Node 内置 `node:sqlite`。
- `node --input-type=module - <arg>` 的脚本参数通常在 `process.argv[2]`，不是 `argv[1]`。
- 参数下标写错曾在工作区误建名为 `-` 的空文件。运行临时脚本前验证绝对路径，清理时只删除精确匹配的临时目标。

### 8.8 密钥可用不等于模型可用

- 用户密钥测试通过，只证明密钥/中转的测试请求通过。
- 生成还要求管理员已经开放对应模型。
- 排障时分别检查：用户密钥 configured 状态、管理员地址、开放模型、当前节点模型、请求路由和上游响应。
- 不要把“没有开放模型”误判为密钥故障。

### 8.9 连接方向与生成结果边

- 手工上下文边使用 `data.relation = 'context'`。
- 自动生成结果边使用 `data.relation = 'result'`。
- 旧 React Flow `xy-edge__*` 手工边要继续兼容。
- 修改画布持久化、复制、撤销或导入时，必须保留边角色；否则参考图可能再次失效或结果节点回流。

### 8.10 Windows 换行提示

- `git diff --check` 可能输出“LF will be replaced by CRLF”。
- 这是转换提示，不等于 whitespace error。仍需确认命令退出码为 0。

### 8.11 隔离验收清理

- 使用虚构账号、虚构密钥、临时数据库和回环假中转。
- 不复用真实 `3102/5182` 数据，不打开真实画布草稿。
- 临时服务使用其他端口；启动前检查占用。
- 清理前解析并验证临时目录绝对路径，核对监听端口和 PID，只停止已识别的夹具进程。
- 清理后复核临时端口无监听，真实 `3102/5182` 仍健康。

### 8.12 生产域名不是只改 DNS

- `api.bkbk.baby` 历史上解析到了错误地址。目标 ECS 上还存在 `Beaver` 的 80/443 冲突和 TLS 问题。
- DNS、应用健康、Nginx/站点占用和证书是独立验收门。
- 未做可逆备份和获得明确生产授权前，不替换现有 80/443 站点。
- 不能只改 DNS 就宣布修复。

## 九、标准目标工作流

1. 获取或创建目标。
2. 定义可观察成功标准和明确不做的事情。
3. 记录基线：HEAD、工作树、相关测试、端口和数据边界。
4. 在 `GOALS.md`/状态文件登记开始、下一步、阻塞和证据。
5. 读取实际代码和已有模式。
6. 在编辑前告诉用户准备修改哪些行为和文件。
7. 使用最小范围实现，避免无关重构。
8. 先跑聚焦测试，再按风险扩大。
9. 需要界面验收时使用隔离环境完成桌面、移动、深浅主题、console 和网络检查。
10. 清理临时服务、数据库、标签页和视口。
11. 运行最终门禁并检查 Git 差异/秘密边界。
12. 按持续交接检查点协议更新目标、计划、状态、修改文件、验证和恢复命令。
13. 如果下一阶段可能超过当前上下文，在继续前先写中途检查点。
14. 只有所有必要工作和最终检查点完成后才标记目标完成。
15. 用户要求推送时，正常提交/推送并验证远端；生产部署保持独立。

## 十、验证门禁

普通实现完成前必须运行：

```powershell
npm.cmd run build
npm.cmd test
git diff --check
```

附加要求：

- 后端/数据库：运行相关聚焦测试、Node 语法检查；数据库变更验证迁移、`quick_check`、`foreign_key_check` 和 `server/check-db.js`。
- 部署脚本：使用 Git 自带 Bash 运行 `bash -n`，并执行生产契约测试。
- 视觉/交互：桌面与 `390x844` 移动端、深浅主题、页面溢出、console warning/error、失败请求和实际命中层。
- 中转：成功、认证失败、未开放模型、超时、过大响应、重试、幂等、退款和秘密反射。
- 推送：fetch 后确认本地和远端完整 SHA 相等。

## 十一、画布草稿与版本冲突

“服务器存在更新”通常表示同一张画布的乐观版本冲突，不是 Git 更新或应用升级。

安全顺序：

1. 不要立即选择服务器版本。
2. 先下载本地草稿 JSON。
3. 确认文件存在且非空。
4. 确定放弃本地内容后，才加载服务器版本。
5. 想保留两版时，把草稿导入另一张新画布或有意识合并。

“使用服务器版本”只影响当前画布的浏览器草稿和加载内容，不会删除源码、Git、账号、资产或其他画布。导入草稿同样会覆盖当前画布，执行前先备份。

## 十二、部署边界

- GitHub 分支：`codex/infinite-canvas`。
- 正常生产更新由用户在 MobaXterm 的 `h` 面板使用第 5 项安全更新。
- 更新流程必须保留 `.env`、数据卷和 `/srv/canvas-backups`，创建并验证更新前备份，只接受快进更新。
- 生产更新后再核对部署 SHA、数据库、容器健康、`/api/health`、公共页面和本轮核心工作流。
- 当前 Windows 机器没有完整 Docker/Nginx/Certbot/UFW 集成环境，不能用本机静态检查代替真实 Ubuntu/Debian 主机验收。
- 未经用户明确指令，不控制 SSH、不更新生产、不修改 DNS/80/443。

## 十三、完成定义

任务只有同时满足以下条件才算完成：

- 请求行为已在正确所有权边界实现。
- 用户已有修改和真实数据完整保留。
- 聚焦与完整自动化通过。
- 必要的浏览器/运行验收完成。
- 临时夹具和进程已清理。
- 没有秘密、用户数据或真实密钥泄露。
- Git 差异只包含目标范围。
- 状态、目标、计划和长期决定已同步。
- 最终持续交接检查点已落盘，包含固定字段和可执行恢复命令。
- 最终说明区分本地实现、GitHub 交付和生产部署。

上下文不足、AI 会话结束、目标预算耗尽或进程重启都不等于任务完成。

## 十四、新会话接班报告模板

新会话完成只读接班后，可以按以下形式向用户报告：

```text
已完成项目接班检查。

- 分支/HEAD：...
- 本地与远端：...
- 工作树：干净 / 包含哪些未提交修改
- 最近交付：...
- 生产状态：...
- 本地服务：3102/5182 ...
- 当前目标：...
- 阻塞/风险：...

Codex 目标模式已启用；后续任务会先建立或恢复目标，再实施、验证和记录。
```

不要在接班报告中复述秘密、真实数据库内容、旧会话 ID 或无关历史。
