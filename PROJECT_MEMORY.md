# 无限画布项目记忆

> 用途：保存用户在历史项目线程中明确表达的目标、偏好、术语和待确认事项。
> 最后整理：2026-07-31，Asia/Shanghai。
> 本文不是代码事实源。开始工作时仍须检查 `git status`、`git diff`、实际代码、测试和浏览器状态。

## 一、记忆使用规则

1. 用户最新的明确要求始终优先于本文。
2. 本文只记录用户明确说过或反复确认过的内容；AI 推断必须单独标注。
3. 历史要求与当前实现冲突时，不根据旧聊天擅自重构，先把冲突告诉用户并确认。
4. 不保存、复述或恢复历史 API 密钥、密码、令牌和完整认证信息。
5. 新出现的长期偏好、产品决定或重要纠正，应更新本文；短期进度写入 `STATUS.md`。
6. 只要一个想法已经确定要执行，就先创建可恢复的活动目标，再按目标拆分、实施和验收；这样线程或 AI 中断后可以从目标与状态文件继续。

## 二、用户术语速查

| 用户提法 | 在本项目中的含义 | 首先查看 |
| --- | --- | --- |
| “三个小弟” | 历史上的三个视觉协作角色；线程现已取消置顶，只保留当前指挥官主线程置顶 | 本文第七节、`coordination/STATUS-*.md` |
| “中枢大脑” | 当前主线程，负责拆分、协调、集成、浏览器验收和公共记忆维护 | `HANDOFF.md`、`STATUS.md` |
| “中转站” | 文字、图片、视频模型的第三方兼容 API 及计费/密钥管理 | 本文第五节、`HANDOFF.md` |
| “八张图” | 参考仓库提供的八张视觉效果图，不是要一次生成八张图片 | 本文第六节、`SPEC.md` |
| “更新会不会覆盖” | 多数情况下指画布服务器版本与浏览器本地草稿冲突，不等于 Git 或应用升级 | `HANDOFF.md` 第十二节 |
| “以目标的形式继续” | 按已确认的项目目标持续推进，不重复历史分析；从状态文件和实际差异恢复 | `STATUS.md`、`PLAN.md` |

## 三、长期产品目标

用户最初明确要求：

- 制作可真正使用的无限画布，而不是静态展示页。
- 最终部署到用户自己的服务器，并通过域名访问。
- 接入第三方 AI 中转站，至少同时支持文字模型和图片生成模型。
- 采用积分制付费；用户可通过充值或兑换码增加积分。
- 管理员需要能够管理中转配置、积分和商业流程。
- 画布应支持真实的节点编辑、AI 生成、资产管理和持久化。

当前产品名为“墨屿画布”。现有商业和数据能力不得为了视觉对齐而被删除。

## 四、用户工作偏好

- 默认使用中文沟通。
- 用户希望 AI 主动完成实现、运行验证并展示结果，不要只给建议或停留在分析。
- 恢复任务时先看 Git 差异和状态文件，不重新分析整段历史对话。
- 用户允许最多同时启用 10 个子智能体；只应为边界清晰、真正独立的工作启用，实际数量受当前平台并发槽位限制，不为凑数量开线程。
- 用户把主线程定义为“指挥官”。指挥官应把边界清晰的工作交给合适的协作线程或子智能体，自己负责协调、跨文件整合、风险控制和最终验收；默认串行激活。
- 子智能体统一使用 `gpt-5.6-sol` 模型和 `Ultra` 推理强度，除非用户当次明确指定其他设置。
- 只置顶当前指挥官主线程；协作线程和以后新建的子智能体不要置顶。2026-07-31 已取消已知协作/子线程置顶，并将主线程 `019fb394-3b22-7111-87ad-f4ae56f97411` 置顶。
- 指挥官根据任务独立性和平台容量决定实际并发数，最多 10 个；共享文件或存在先后依赖的任务仍必须串行。
- 并行角色积极工作时约每两分钟记录一次进度、下一步、阻塞和证据。
- 已确认准备执行的想法必须先登记为目标，并把每一步作为可恢复的目标步骤推进；停止前同步目标现状、已完成、下一步、阻塞和验证证据。
- 长任务应以明确目标持续推进；上下文过长时，用项目记忆和状态文件开启新线程，不在超限线程中反复重试。
- 图片和参考截图必须一张一张处理、检查和展示。不得一次加载或输出八张图。
- 用户要求联网搜索时，优先使用百度等可访问来源，不要依赖 Google。
- 用户要求“看一下”界面时，应启动或复用本地服务并在内置浏览器中直接展示。
- 不得重置、清理、检出覆盖或删除工作区中已有的重要修改。
- 对覆盖、升级、导入、切换服务器版本等操作，要先说明具体会影响代码、服务器数据还是浏览器本地草稿。

## 五、中转站记忆

### 已明确的需求

- 文字与生图中转的 API 地址、密钥和模型可能不同，必须支持独立配置；视频也已按独立配置方向实现。
- 管理员面板需要能够设置和测试中转站地址、密钥与模型。
- 产品不能只有聊天，必须能正常生图；文字模型与图片模型都需要。
- 生图出现 `401` 时，应检查请求使用的中转类型、地址、密钥、模型与认证格式，不把它简单归因于前端。
- 历史上用户提供过测试地址、模型和密钥用于连通性测试。密钥属于秘密，本文和任何其他文档都不得恢复、记录或复述它。

### 已确认采用的方案

- 2026-07-31 用户明确选择较晚方案：图片生成端点锁定为 `https://www.bkbk.baby/`，用户只输入自己的图片 API 密钥，图片费用由中转站处理，不扣站内图片积分。
- 文字与视频链路继续使用当前独立配置和站内计费，不随图片方案一起改变。
- 用户图片密钥只在服务端加密保存；浏览器只能看到“是否已配置”，不能读取密钥。

较早的“管理员共享图片密钥并按站内积分扣费”方案已被此次明确选择取代，不得在没有用户新指令时恢复。

### 安全边界

- 中转密钥不得进入源码、持久化前端状态、文档、截图、日志或线程消息。用户输入自己的图片密钥时，可短暂存在密码输入框的组件 state 和请求体中，但请求结束必须清空，且不得进入 localStorage 或画布草稿。
- 共享密钥只能保留在服务端并加密存储；浏览器只能看到是否已配置和允许选择的模型。
- 测试操作不得在输出中回显请求头、完整密钥或可恢复的密钥片段。
- 视频结果只能从视频中转站自身 origin 或 `AI_VIDEO_MEDIA_ORIGINS` 明确列出的精确 origin 下载。每一跳重定向都必须重新校验来源与 DNS/IP；视频中转认证头只能发往中转站自身 origin，不能带到外部媒体域名。
- 异步视频结果因用户媒体配额返回 `413` 时，该任务应进入失败状态并且只退款一次；媒体下载型 `502/504` 视为可能恢复的瞬时错误，任务保持 pending 以便后续轮询重试。
- `JWT_SECRET` 同时用于登录令牌、文字/视频中转密钥、用户图片密钥和媒体签名。当前没有密钥轮换迁移机制；数据库备份与恢复必须使用对应的原值，并由 `server/check-db.js` 验证密文可解密。

## 六、视觉与交互偏好

- 用户指定 `basketikun/infinite-canvas` 作为重要的视觉与交互参考，认为其完成度明显优于早期版本。
- 参考仓库是 AGPL-3.0，只作为行为和视觉基准，不盲目复制源码。
- 八张参考图必须逐张对照：一次检查一张、完成或验证相关修改后再看下一张。
- 登录页左侧已有的“梳理创意 / 让 AI 延展思路 / 收集视觉参考”内容不能移除。
- 登录页左侧图形曾被明确评价为生硬，应向更有创意、个性且简洁的图片表达改进，同时保留原有内容含义。
- 图片节点选中或悬停时应提供完整快捷工具；裁剪、切图和放大必须产生可继续编辑的新节点，不能只做装饰按钮。
- 连接线应是优雅的曲线，并提供明确的删除能力。
- 现有图片工具、账号、积分、管理员、中转站和草稿兼容性不能因视觉改造而回退。

## 七、协作结构

用户历史上建立过三个视觉协作线程，每个线程负责一个部分，由主线程统一验收：

1. “无限画布 1 - 画布外壳对齐”
   负责 `src/styles/canvas-shell-reference.css` 与 `coordination/STATUS-shell.md`。
2. “无限画布 2 - 底部控制区对齐”
   负责 `src/styles/canvas-controls-reference.css` 与 `coordination/STATUS-controls.md`。
3. “无限画布 3 - 节点视觉层对齐”
   负责 `src/styles/canvas-nodes-reference.css` 与 `coordination/STATUS-nodes.md`。

当前主线程是中枢协调和最终验收角色。三个历史线程的成果已归档，线程不再置顶；需要新任务时按当前目标重新分配，不要仅因历史名称自动激活。

指挥官验收流程：

1. 先创建活动目标并写入 `STATUS.md`。
2. 将真正独立的实现登记给合适的协作线程或子智能体，明确文件所有权和验证要求；默认一次只激活一个，只有用户当次明确批准时才增加并发。
3. 主线程持续检查状态与实际 Git 差异，不只相信线程摘要。
4. 工作线程完成后，主线程统一检查跨文件契约、运行构建和完整测试。
5. 涉及界面时，由主线程在内置浏览器逐个场景验收，参考图片一次只处理一张。
6. 检查控制台、失败请求、密钥泄露、草稿与版本冲突风险。
7. 更新目标、`STATUS.md`、`DECISIONS.md` 和必要的长期记忆；只有所有门禁完成后才宣布目标完成。

## 八、端口与展示约定

- 当前前端：`http://127.0.0.1:5182/`。
- 当前 API：端口 `3102`，健康检查为 `/api/health`。
- 用户曾要求避开当时被占用的端口；本项目不得占用 `3000` 或 `5174`。
- 登录界面可用独立来源展示，避免清除原画布会话；登录后的工作区使用 `127.0.0.1:5182`。

## 九、画布草稿与版本冲突

页面提示“服务器存在更新”时：

- 这通常是画布文档版本冲突，不是 Git 更新，也不是程序升级。
- 本地草稿可能只存在浏览器存储中。
- 在下载或确认备份本地草稿前，不得点击“使用服务器版本”。
- “使用服务器版本”只会影响当前画布的浏览器本地草稿与加载内容，不会删除源码、Git 修改、账号、积分或其他画布。
- 导入草稿也会替换当前画布内容，执行前同样需要备份。

## 十、历史记录来源与更新方法

本次整理读取了本项目目录下可访问的历史主开发线程、上下文超限排障线程、三个当时曾置顶的执行线程，以及现有交接/状态文件。主要历史线程：

- 主开发线程：`019fb2e7-7166-7460-bd4c-ba69222f508f`
- 上下文超限排障：`019fb38b-9903-7dc0-92c3-8872adb23643`
- 画布外壳：`019fb301-c0cf-7700-ac8d-e9c56c5f6b69`
- 底部控制：`019fb301-e067-7891-b7dd-23cde5f2a577`
- 节点视觉：`019fb301-a1a3-7850-bb46-41a2b22ed245`

更新本文时：

1. 只加入会影响以后决策的长期信息。
2. 写明是“用户明确要求”“当前代码事实”还是“待确认冲突”。
3. 不复制整段对话、工具输出、临时路径或秘密。
4. 实现完成情况仍写入 `STATUS.md`；稳定技术决定写入 `DECISIONS.md`。
